<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProjectMapper">
  
  <resultMap id="projectMap" type="com.eomcs.pms.domain.Project">
    <id column="no" property="no"/>
    <result column="title" property="title"/>
    <result column="content" property="content"/>
    <result column="sdt" property="startDate"/>
    <result column="edt" property="endDate"/>
    
     <association property="owner" javaType="com.eomcs.pms.domain.Member">
     <id column="owner_no" property="no"/>
     <result column="owner_name" property="name"/>
    </association>
    
    <collection property="members" ofType="com.eomcs.pms.domain.Member">
      <id column="members_no" property="no"/>
      <result column="members_name" property="name"/>
    </collection>
  </resultMap>
  
  <!-- 
  <select id="findAllMembers" resultMap="memberMap" parameterType="int">
    select
      m.no, m.name
    from pms_member_project mp
      inner join pms_member m on mp.member_no = m.no
    where
      mp.project_no = #{no}
  </select>
   -->
  
  <insert id="insert" parameterType="com.eomcs.pms.domain.Project" useGeneratedKeys="true" keyColumn="no" keyProperty="no">
    insert into pms_project(title, content, sdt, edt, owner)
    values(#{title}, #{content}, #{startDate}, #{endDate}, #{owner.no})
  </insert>
  
  <insert id="insertMember" parameterType="map">
    insert into pms_member_project(member_no, project_no)
    values(#{member}, #{project})
  </insert>
  
  <select id="findAll" resultMap="projectMap">
    select 
      p.no, p.title, p.sdt, p.edt, m.no owner_no, m.name owner_name, membersno
    from pms_project p
      inner join pms_member m on p.owner=m.no
      inner join pms_member_project mp on m.no=mp.member_no
    order by p.title asc;
  </select>
  
  <select id="findAllMembers" parameterType="int">
    select
      m.no, m.name
    from pms_member_project mp
      inner join pms_member m on mp.member_no = m.no
    where mp.project_no = #{no}
  </select>
  
  <select id="findByNo" resultMap="projectMap" parameterType="int">
    select
      p.no, p.title, p.content, p.sdt, p.edt, m.no owner_no, m.name owner_name
    from pms_project p
      inner join pms_member m on p.owner = m.no
    where p.no = #{no}
  </select>
  
  <update id="update" parameterType="com.eomcs.pms.domain.Project">
    update pms_project set
        title = #{title}, 
        content = #{content},
        sdt = #{startDate},
        edt = #{endDate},
        owner = #{owner.no}
    where no = #{no}
  </update>
  
  
  <delete id="delete" parameterType="int">
    delete from pms_project
    where no = #{no}
  </delete>
  
  <delete id="deleteMembers" parameterType="int">
    delete from pms_member_project
    where project_no = #{no}
  </delete>
  
  
  
</mapper>







